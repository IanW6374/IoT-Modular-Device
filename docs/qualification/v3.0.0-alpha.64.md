# v3.0.0-alpha.64 portal and managed-upgrade regression

## Automated evidence

- 691 Python tests pass, including architecture boundaries, nested navigation,
  qualification controls and upgrade orchestration.
- The application runtime no longer references
  `release_update.for_release_sequence`, preserving compatibility with an
  already-loaded Alpha 62 core module.
- Portal CSS and JavaScript contracts cover nested submenu pointer, keyboard
  and delegated-click behaviour.

## Device checks

1. Open Maintenance and expand Certificates, Logging and Upgrades using both a
   pointer and keyboard.
2. Confirm **Start scenario** is aligned at the bottom-right of the disruptive
   qualification panel.
3. Run an Automatic upgrade check and confirm no `for_release_sequence`
   failure is recorded in Upgrade history.
4. Push a targeted release from IoT-MD Management and confirm the device queues
   the exact release sequence, stages it and observes the configured activation
   window.

Device and browser observations remain pending and must be recorded separately
from this automated evidence.
