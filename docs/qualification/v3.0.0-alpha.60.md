# Alpha 60 fleet policy epoch test

Alpha 60 uses release sequence `2765` and native platform ABI 6.

Status: **host regression passed; awaiting device evidence**.

1. Upgrade the application to Alpha 60 and confirm a healthy startup while the
   Alpha 52 native core remains installed.
2. Apply a newly signed policy from Management Suite 2.2.11.
3. Confirm the request returns HTTP 202 rather than `fleet policy is not
   currently valid`.
4. Confirm `GET /api/v2/fleet` reports a non-zero `policy_sequence` and the
   selected maintenance window.
5. Confirm the check and download commands select the policy's exact release
   sequence from its assigned channel.
6. Confirm an overnight maintenance selection leaves activation pending before
   its local start time, then installs within the window.
