# IoT-MD v3.0.0-alpha.13 qualification

Alpha 13 uses release sequence `2718` and native platform ABI 6.

## Required observations

1. Install the universal artifact from the recovered Alpha 10/Alpha 9 pair.
2. Verify update history records `pair_state_upgraded` before
   `runtime-healthy`, `platform-confirmed`, `platform-metadata` and
   `runtime-committed`.
3. Verify both displayed component versions finish on Alpha 13 and remain there
   after a normal restart and power cycle.
4. Interrupt power before and after native confirmation. Each interruption
   must finish on either the complete Alpha 13 pair or the complete prior pair.
5. Repeat the stale-journal, forced-confirmation-failure, recovery, network,
   identity, driver, migration, shadow and soak checks from Alpha 12.

Active-v3 promotion remains blocked until every release-bound gate has passing
HIL evidence.
