# v3.0.0-alpha.66 portal interaction refinement

## Automated evidence

- 693 Python tests pass, including update workflows, portal contracts,
  accessibility checks, architecture boundaries and signed-update coverage.
- Chromium exercises the Update workflow at 2048, 1440, 1024, 768 and 390
  pixels without page errors or content-security-policy violations.
- Browser checks confirm update discovery remains in place, Automatic and
  Manual controls follow the same order, and the final milestone renders one
  opaque Restart and install action.
- Portal contracts verify the simplified navigation, the log-level status badge
  and the flat Device API action layout.

## Device checks

1. Run Check for updates and confirm the result appears without opening another
   page or tab.
2. Select both Automatic and Manual updates and confirm their release/file
   details and Start update actions follow the same order.
3. Stage an update and confirm Restart and install is rendered as one primary
   control without a progress line crossing its label.
4. Confirm the Device log badge shows the selected level, live or paused state,
   and refresh time without a separate successful-change message.
5. Confirm Device API save and discard actions appear directly in the listener
   panel and remain functional.

Device observations remain pending and must be recorded separately from this
automated evidence.
