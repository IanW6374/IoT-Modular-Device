# v3.0.0-alpha.65 update UX and typed fleet deployment

## Automated evidence

- 693 Python tests pass, including architecture boundaries, update UI,
  qualification retries, fleet-policy validation and signed-message coverage.
- Fleet policy formats 1 and 2 are accepted; format 2 signs and validates the
  requested Application, Core or Universal artifact type.
- A Canary Health reset is rejected while the fleet pause is active, archives
  the failure after the pause is cleared, and returns the gate to not-run.
- Portal contracts verify delayed pointer submenu expansion, immediate
  log-level submission and the absence of the stored-line control from the live
  Device log.

## Device checks

1. Confirm Maintenance shows **Updates**, and that pointer submenus open only
   after a short hover while click and keyboard navigation still respond.
2. On Device log and Audit log, confirm the live/paused badge is beside the
   title and displays the most recent refresh time.
3. Change Device log level and confirm it applies without an Apply button or a
   page redirect. Confirm Stored lines remains under Device > Logging.
4. Clear an active fleet pause, reset a failed Canary Health gate, and confirm
   its prior failure is retained while the gate returns to not-run.
5. From Management Suite, deploy each available Application, Core and Universal
   artifact and confirm the device checks and stages only the selected type.

Device and browser observations remain pending and must be recorded separately
from this automated evidence.
