# Alpha 58 deferred peer-identity test

Alpha 58 uses release sequence `2763` and native platform ABI 6.

Status: **request dispatch passed; error response failed on device**. Device
event 1657 confirmed that the Home Assistant POST reached the fleet policy
handler. The unavailable `PermissionError` name then broke exception matching
while handling the expected policy validation failure. Superseded by Alpha 59.

1. Upgrade the application to Alpha 58 and confirm a healthy startup while the
   Alpha 52 native core remains installed.
2. Confirm `GET /api/v2/fleet` returns HTTP 200 with the enrolled client.
3. POST `{}` to `/api/v2/fleet/policy` and confirm HTTP 400 JSON is returned;
   the connection must not close without a response.
4. Push a signed policy from Management Suite 2.2.10 or later.
5. Confirm the Suite reports success and `GET /api/v2/fleet` shows a non-zero
   `policy_sequence`.
6. Repeat a qualification-event POST and confirm it returns a bounded API
   response rather than EOF.
