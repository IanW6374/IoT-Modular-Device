# Alpha 43 continuous staged-workflow test

Alpha 43 uses release sequence `2748` and native platform ABI 6.

1. Select a manual file and confirm its labelled filename panel is visually
   distinct beneath **Select signed file** and Discard uses the destructive
   colour.
2. Select an automatic release and confirm the selected version is prominent
   beneath **Select version**.
3. Complete verification and confirm the page changes to the staged workflow
   without a full reload or visible navigation.
4. Confirm the selected release and Discard remain beneath **Release selected**,
   Verified remains beneath **Verify and stage**, and restart remains in the
   final step.
5. Leave and reopen Upgrade and confirm the same staged layout is rendered.
6. Restart and verify healthy confirmation and retained rollback operation.
